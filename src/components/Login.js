import LoginPage from "./LoginPage";

const Login = () => {

    return (
        <LoginPage
          text='Регистрация'
          title='Вход'
          btn='Войти'
        />
    );
};

export default Login;
